import React, { useEffect } from "react";
import { Link } from "react-router-dom";

const Checkout = () => {
  return <div>{"There is no checkout because this is not the real shop"}</div>;
};

export default Checkout;
